
<h1>Dogs</h1>

<%= link_to 'New Dog', new_dog_path %>

<br>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Real Name</th>
            <th>Call Name</th>
            <th>DOB</th>
            <th>Breeder</th>
            <th>Owner</th>
            <th>Handler</th>
            <th>Sex</th>
            <th>Test Results</th>
            <th>Parentage</th>
            <th>Status</th>
            <th>Litter</th>
        </tr>
    </thead>

    <tbody>
        <% @dogs.each do |dog| %>
            <tr>
                <td><%= dog.rname %></td>
                <td><%= dog.cname %></td>
                <td><%= dog.dob %></td>
                <td><%= "breeder placeholder" %></td>
                <td><%= dog.owner %></td>
                <td><%= dog.handler %></td>
                <td><%= dog.sex %></td>
                <td><%= "test placeholder" %></td>
                <td><%= "parentage placeholder" %></td>
                <td><%= "status placeholder"%></td>
                <td><%= "litter placeholder" %></td>
                <%# remove hover and active effects on buttons at some point %>
                <td><%= link_to 'Show', dog, type: "button", class: "btn btn-success" %></td>
                <td><%= link_to 'Edit', edit_dog_path(dog), type: "button", class: "btn btn-warning" %></td>
                <td><%= link_to 'Destroy', dog, method: :delete,
                data: { confirm: 'Are you sure?' }, type: "button", class: "btn btn-danger" %></td>
            </tr>
        <% end %>
    </tbody>

</table>
